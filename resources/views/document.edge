<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
  <title>Correio da Lavoura OCR - {{ fileName }}</title>
</head>
<body class="side-by-side">
  <div class="text">
    <div class="controls">
      @if(previousPage)
        <a href="/document/{{fileName}}/{{previousPage}}">Anterior</a>
      @endif
      {{ pageNumber }}
      @if(nextPage)
        <a href="/document/{{fileName}}/{{nextPage}}">Próxima</a>
      @endif
    </div>
    <iframe src="{{ text }}" frameborder="0"></iframe>
  </div>
  <div class="image">
    <img src="{{ image }}" alt="">
  </div>
</body>
</html>
